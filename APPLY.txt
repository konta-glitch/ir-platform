PRIMENA — Paralelizacija narativa
==================================
Iz korena ir-platform-clean:
  git checkout main && git pull origin main
  git checkout -b feat/parallel-narrative
  unzip -o ~/Downloads/parallel-narrative.zip -d .
  rm -f APPLY.txt
  git add -A && git commit -m "perf: parallelise narrative LLM pass + full-severity scope"
  git push -u origin feat/parallel-narrative
Pa PR -> merge. Posle merge-a: docker compose restart backend
